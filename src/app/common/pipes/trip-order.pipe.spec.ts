import { TripOrderPipe } from './trip-order.pipe';

describe('TripOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new TripOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
